namespace AgarioModels
{
    public class Player : GameObject
    {
        public string Name { get; set; }
        /*        public Player(string name, int ID, System.Numerics.Vector2 pos, int ARGBColor, float Mass) : base(ID, pos, ARGBColor, Mass)
                {
                    Name = name;
                }

                public Player(string name, int X, int Y, int ARGBColor, int ID, float Mass) : base(ID, X,Y, ARGBColor, Mass)
                {
                    Name = name;
                }*/
    }
}