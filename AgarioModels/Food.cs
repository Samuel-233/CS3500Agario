using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.Json.Serialization;
using System.Threading.Tasks;

namespace AgarioModels
{
    public class Food : GameObject
    {

        

/*        public Food(int ID, System.Numerics.Vector2 pos, int ARGBColor, float Mass) : base(ID, pos, ARGBColor, Mass)
        {
            radius = (float)Math.Sqrt(Mass / 3.14159f);
        }

        [JsonConstructor]
        public Food(int X, int Y, int ARGBColor, int ID, float Mass) : base(ID, X, Y, ARGBColor, Mass)
        {
            radius = (float)Math.Sqrt(Mass / 3.14159f);
        }
*/
    }
}
